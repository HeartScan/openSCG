properties:
  template:
    containers:
      - name: openscg-server
        image: openscgregistry.azurecr.io/openscg-server:latest # Placeholder, will be replaced by CI/CD
        resources:
          cpu: 0.25
          memory: 0.5Gi
    scale:
      minReplicas: 0
      maxReplicas: 1
